"""Standalone, offline scripts for FOV-selection model development.

These are batch/CLI tools (feature extraction, projection stores, goodness
prediction) used to build and evaluate the FOV-selection model offline. They
are NOT part of the online acquisition path (see ``shrimpy.fov_selection`` for
that). Run them with ``python -m shrimpy.scripts.<name>``.
"""
